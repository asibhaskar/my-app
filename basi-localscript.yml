---
  - hosts: all
    become_user: root
    tasks:
      – name: Copy script collect_audit.sh
          copy:
          src: /home/ansible/collect_audit.sh
          dest: /home/ansible/collect_audit.sh
          owner: ansible
          mode: 0744      
      - name: Run script for audit
          command: sh /home/ansible/collect_audit.sh
      
